
function Header() {
    const headerBar = document.createElement("header");
    para.textContent = <a>test</a>;

    <header class="header">
        <a href="login" className="login">
            Sign Up/Login
        </a>
    </header>    
    document.body.appendChild(headerBar);
}

